import React from 'react';

import './Navbar.css'

const Navbar = () => (
    <header>
        <nav className="navbar">
            <div className="navbar-header">
                <a className="navbar-brand" href="/">share eat</a>
            </div>
        </nav>
    </header>
);

export default Navbar;